# TODO
- [x] Replace Puppeteer import and launch config in `server/utils/tracerPdf.ts`.
- [x] Remove now-unused executable detection code in `server/utils/tracerPdf.ts`.
- [x] Update `server/package.json` dependencies to use `puppeteer-core` and `@sparticuz/chromium` (and remove `puppeteer` if appropriate).

- [x] Run TypeScript build / lint checks.




